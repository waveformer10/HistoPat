type InputVariant = "primary" | "secondary";

export type InputProps = {
  disabled?: boolean;
  label?: string;
  placeholder?: string;
  isPassword?: boolean;
  isSearch?: boolean;
  isMultiple?: boolean;
  borderStyle?: "secondary";
};
